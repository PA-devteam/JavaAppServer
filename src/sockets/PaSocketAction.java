package sockets;

public enum PaSocketAction {
    // Connection (connexion)
    LOGIN,
    // Register
    REGISTER,
    // Disconnection (déconnexion)
    LOGOUT,
    // Miscellaneous (divers)
    MISC,
    // Init 
    INIT ,
    //Updateequation
    UPDATEEQUATION,
    //CreateEquation,
    CREATEEQUATION,
    //DeleteEquation
    DELETEEQUATION
}

